services:
- type: web
  name: autotrend_vault-bot
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
  envVars:
  - key: BOT_NAME
    value: autotrend_vault
  - key: TASKS_URL
    value: https://raw.githubusercontent.com/REPLACE_ME/archland/main/tasks.json
  - key: AUTH_TOKEN
    generateValue: true
  - key: SHIFT_START
    value: "20:00"
  - key: SHIFT_END
    value: "00:00"
